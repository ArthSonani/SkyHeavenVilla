This folder should contain the static public assets used by Vite, e.g. img1_.jpg, work_.png.
Currently the original files are still in `frontend/public/`. Move them here if you want them at project root.
